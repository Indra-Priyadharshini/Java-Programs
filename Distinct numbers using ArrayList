/*Write a program using ArrayList, that prompts the user to enter a sequence of numbers, stores in a ArrayList and displays the distinct numbers in the sequence. 
Assume that the input ends with 0 and 0 is not counted as a number in the sequence.*/


import java.util.*;
class Main{
    public static void main(String s[]){
    ArrayList a=new ArrayList(20);
    Scanner ss=new Scanner(System.in);
    int x;
    while((x=ss.nextInt())!=0){
        if(!(a.contains(x)))
        a.add(x);
    }
    
    for(int i=0;i<a.size();i++)
    System.out.print(a.get(i)+" ");
}}
